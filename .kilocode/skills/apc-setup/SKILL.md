---
name: apc-setup
description: Guided first-run APC setup — toolchain, paths, models, JUCE verification. Trigger with /apc-setup or natural language "set up APC".
---

> **APC pointer** - canonical file: `.agents/skills/apc-setup/SKILL.md`.

Load and follow `../../.agents/skills/apc-setup/SKILL.md` exactly as written (it contains the full instructions, validation steps, and completion message).