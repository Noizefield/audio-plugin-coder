---
description: "Open the APC Hub live dashboard (read-only command center)"
---

> **APC pointer** - canonical file: `.agents/workflows/apc-hub.md`.

Load and follow `../../.agents/workflows/apc-hub.md` exactly as written (it contains the full instructions, validation steps, and completion message).
