# Copy or merge into ~/.codex/ as a named profile (see docs/codex-orchestration.md).
# Example: copy to ~/.codex/luna.config.toml and run: codex exec --profile luna "…"

model = "gpt-5.6-luna"
model_reasoning_effort = "low"
