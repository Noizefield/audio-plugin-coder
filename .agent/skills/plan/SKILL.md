> **APC pointer** - canonical file: `.agents/skills/plan/SKILL.md`.

Load and follow `../../.agents/skills/plan/SKILL.md` exactly as written (it contains the full instructions, validation steps, and completion message).