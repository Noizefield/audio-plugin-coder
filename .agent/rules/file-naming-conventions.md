> **APC pointer** - canonical file: `.agents/rules/file-naming-conventions.md`.

Load and follow `../../.agents/rules/file-naming-conventions.md` exactly as written (it contains the full instructions, validation steps, and completion message).