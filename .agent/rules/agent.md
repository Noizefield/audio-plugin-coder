> **APC pointer** - canonical file: `.agents/rules/agent.md`.

Load and follow `../../.agents/rules/agent.md` exactly as written (it contains the full instructions, validation steps, and completion message).