> **APC pointer** - canonical file: `.agents/skills/ship/SKILL.md`.

Load and follow `../../.agents/skills/ship/SKILL.md` exactly as written (it contains the full instructions, validation steps, and completion message).